クエリ

SELECT
	u.l_name || u.f_name
FROM
	usr AS u
EXCEPT
SELECT
	q.name
FROM
	quest AS q
;

実行結果
MariaDB [workbook]> SELECT
    -> u.l_name || u.f_name
    -> FROM
    -> usr AS u
    -> EXCEPT
    -> SELECT
    -> q.name
    -> FROM
    -> quest AS q
    -> ;
+----------------------+
| u.l_name || u.f_name |
+----------------------+
| 0                    |
+----------------------+
1 row in set, 42 warnings (0.003 sec)