クエリ
間違い：
・CREATE TABLEのあとにINTOがいらない
・PRIMARY KEYを作成する際、カンマが間にあった

CREATE TABLE
	rental
	(
	id INT AUTO_INCREMENT,
	user_id CHAR(7),
	isbn CHAR(13),
	rental_date DATE
	returned SMALLINT DEFAULT 0,
	PRIMARY KEY(id)	
	)
;

実行結果
MariaDB [workbook]> CREATE TABLE
    -> rental
    -> (
    -> id INT AUTO_INCREMENT,
    -> user_id CHAR(7),
    -> isbn CHAR(13),
    -> rental_date DATE,
    -> returned SMALLINT DEFAULT 0,
    -> PRIMARY KEY(id)
    -> )
    -> ;
Query OK, 0 rows affected (0.021 sec)